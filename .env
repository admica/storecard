# =============================================================================
# StoreCard Environment Variables Template
# =============================================================================
# This file contains all environment variables used by the application.
# Copy this file to .env.local and fill in your actual values.
# .env.local is gitignored and will not be committed to the repository.
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Source: Supabase Dashboard > Project Settings > Database
# DATABASE_URL: Connection string for Prisma (uses connection pooling)
# DIRECT_URL: Direct connection string for migrations (no pooling)
DATABASE_URL=
DIRECT_URL=

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Source: Supabase Dashboard > Project Settings > API
# NEXT_PUBLIC_SUPABASE_URL: Your Supabase project URL
# NEXT_PUBLIC_SUPABASE_ANON_KEY: Public anonymous key (safe to expose in client)
# SUPABASE_SERVICE_ROLE_KEY: Service role key (server-side only, keep secret!)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# -----------------------------------------------------------------------------
# Stripe Configuration
# -----------------------------------------------------------------------------
# Source: Stripe Dashboard > Developers > API keys
# STRIPE_SECRET_KEY: Secret key for server-side Stripe API calls (starts with sk_test_ or sk_live_)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: Publishable key for client-side Stripe.js (starts with pk_test_ or pk_live_)
# STRIPE_WEBHOOK_SECRET: Webhook signing secret (starts with whsec_) - Get from Stripe Dashboard > Developers > Webhooks
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Stripe Price IDs
# -----------------------------------------------------------------------------
# Source: Stripe Dashboard > Products > [Your Product] > Pricing
# These are the Price IDs for your subscription tiers
# Create products in Stripe Dashboard first, then copy the Price IDs here
STRIPE_MONTHLY_PRICE_ID=
STRIPE_YEARLY_PRICE_ID=

# -----------------------------------------------------------------------------
# Mailgun Configuration
# -----------------------------------------------------------------------------
# Source: Mailgun Dashboard > Sending > API Keys
# MAILGUN_API_KEY: Your Mailgun API key (starts with key- or is a sending key)
# MAILGUN_DOMAIN: Your Mailgun domain (e.g., sandbox...mailgun.org for testing, or your custom domain)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=

# -----------------------------------------------------------------------------
# NextAuth Configuration
# -----------------------------------------------------------------------------
# Source: Generate with: openssl rand -base64 32
# AUTH_SECRET: Secret key for NextAuth.js session encryption
# Generate a new secret: openssl rand -base64 32
AUTH_SECRET=

# -----------------------------------------------------------------------------
# Optional: Logo API (if using Logo.dev service)
# -----------------------------------------------------------------------------
# Source: Logo.dev Dashboard
# LOGO_DEV_PUBLIC: Public API key for Logo.dev
# LOGO_DEV_SECRET: Secret API key for Logo.dev
# LOGO_DEV_PUBLIC=
# LOGO_DEV_SECRET=
